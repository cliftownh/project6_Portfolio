extends layout

block content 
  article.grid-container.portfolio-intro
    div

      //- Add a title or heading like My Portfolio, or Hey, Potential Employers, or Let's make the web great together! 
      h1 Clifton Hefley

      //- Add a description for your portfolio, what this projects is, it's purpose, what it's suppossed to accomplish, etc. 
      p.lead.text-light
        | Portfolio Description
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      //- Use the Pug `each` iterator to create a projet img, title and link in the markup for each of the projects you are showcasing.  
      
      //- Use a combination of interpolation and template literals as needed to dynamically generate the the routes in the href, as well as the img's src and the projects title.
      
      //- Here's what the Pug for each projects example should look like. 

      .cell
        a(href='projects/0')
          img.thumbnail(src=`${projects[0].image_urls[0]}`)
          h5 #{projects[0].project_name}
      .cell
        a(href='projects/1')
          img.thumbnail(src=`${projects[1].image_urls[0]}`)
          h5 #{projects[1].project_name}
      .cell
        a(href='projects/2')
          img.thumbnail(src=`${projects[2].image_urls[0]}`)
          h5 #{projects[2].project_name}
      .cell
        a(href='projects/3')
          img.thumbnail(src=`${projects[3].image_urls[0]}`)
          h5 #{projects[3].project_name}
      .cell
        a(href='projects/4')
          img.thumbnail(src=`${projects[4].image_urls[0]}`)
          h5 #{projects[4].project_name}
      
